* TODO test things like typechecking illegal selections
* TODO !!! unique identifiers, shadowing etc
* TODO propagate KR computations and compute them once per 128-sample frame
** for example, having fn f(a: slow i32, b: slow i32) ... then every subexpression that depends on a and b only can be floated to the be a binding in f
* TODO rdtable, rwtable
* DONE shuffle bindings in Gen
* TODO binary tree if
* TODO: allow simple arithmetic in range |i| syntax
* TODO: streams not in scope outside of graph
* TODO: normal functions/methods not in scope in graph (only variables are in scope)
* TODO: branch operation computes both branches
* TODO: after the shadow check/SSA pass all Idents are unique
* TODO: have --max-locals/--max-globals options; if exhausted, go to mem
* TODO: should this be legal: f: f32[4] -> f32, rec |prev| return (f prev)
* TODO typecheck
** is it possible to only type annotate arguments (and maybe return values) and have everything else be inferred?
* TODO var names -> indices, SSA
* DONE check for recursion
** no mutual recursion in graph code
* TODO mutual recursion in imperative code only with `wait` inbetween
* TODO constant folding (also fold compile time constants like $voices)
* TODO compiler time expression expansion (range, fold, lvalue index expressions must be computable at compile time)
* TODO cluster common subexpressions
* TODO if cluster referenced only once, inline
** have a configurable max inline function size
** optimization must happen after inlining
*** simplify, fusion rules, find fixpoint
**** fusion .e.g fold . map = fold with folded map inside :D
**** (ERec _ _ _ (EConst n)) = n
**** (ESelect [a, b, c])[1] = b
**** (App (Abs f)) = can inline if feasible
**** (Abs _ -> let b = xs in b) = (Abs _ -> xs) (bust this should be handled by inline-if-used-only-once rule)
* TODO if something is not referenced in delay, don't alloc delay box and compute it lazily in e.g. select
** e.g. bindings that do not reference the recusive head can be outside the rec block
* TODO codegen
** choice strategy: for small choice tables (e.g. < 5) use ifs
** two targets: WASM and C
* TODO MCP server with retrieve-syntax, retrieve-type, retrieve-source, retrieve-docs, etc
* TODO LSP server

* TODO: in typechecking, check that static indices are within range
** even better: attach range to index; then check if everything ok in range check
***  otherwise expect a clamp() or wrap() range correcting fun
** if not possible, then demand clamp/wrap in dynamic select index expressions
* TODO: in the CallM monad, arguments that get written to the output can pass their array ctx slice to the argument expression, so no need for copy

* TODO: optimization is performed on the CExpr datatype

* TODO: alignment in AllocM!

* TODO: what happens if part of the return value is a capture?
** this is basically return value ref propagation up the binding chain
** the most recent returned binding (or argument) gets tagged with "write to return value ref"